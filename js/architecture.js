// Square piece
//   - holds the position [row, column]
//   - holds ship info (ship index of array)
//   - marked tile info


// Ship
//   - length of ship
//   - name of ship (shipType)
//   - ex: shipHit [0, 0, 0] by default
//   - will change mark to 1
//   - ship is considered sunk when the array is filled with 1's (have shipSunk variable --> boolean)



//Ship properties:
// shipLength;
// shipType;
// shipHit;


// Square object (within gameboard)
// boardPosition;
// shipObject (? - pass in length of ship, use index to access shipHit)
//

// Init gameboard



// Placing ships on the board



// Ship is placed

// Calculate Points
//   -

// The default is [0]
// This means that a tile is not marked


// var shipSize = 6;

// var createShipMarkedArray = function() {
//   var isNotHit = [];
//   for (var i = 0; i < shipSize; i++) {
//     isNotHit.push(0);
//   }
//   return isNotHit;
// }

// var shipSize = 3;
// assert(createShipMarkedArray().length === [0, 0, 0], "isMarked returns an array of [0, 0, 0] when the shipSize is 3");

// var checkIfMarked = function(array) {

// }


